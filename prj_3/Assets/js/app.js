/*function factorial(x) {
    if (x < 0) {
        throw new Error("Значение x меньше 0");
    }
    let f;
    for(f = 1; x > 1; x--) {
        f *= x;
    }
    return f;
}
try {
    let n = Number(prompt("Введите положительное число", ""));
    let f = factorial(n);
    alert(n + "! = " + f);
} catch (ex) {
    alert(ex);
} */

